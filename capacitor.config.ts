import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nl.joserasmussen.papiamentokids',
  appName: 'papiamento-kids',
  webDir: 'public'
};

export default config;
